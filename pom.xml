<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!--
      ┌──────────────────────────────────────────────────────────────────────┐
      │  認證方法教學 (Authentication Methods Tutorial)                          │
      │                                                                        │
      │  以「測試驅動」的方式學習 SSO / OAuth 2.0 / OIDC / SAML 四種概念。        │
      │  架構：Hexagonal Architecture (Ports & Adapters) + DDD + SOLID         │
      │  技術：JDK 23、Spring Boot 4                                            │
      └──────────────────────────────────────────────────────────────────────┘
    -->

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.7</version>
        <relativePath/>
    </parent>

    <groupId>com.example</groupId>
    <artifactId>authentication-methods-tutorial</artifactId>
    <version>1.0.0</version>
    <name>authentication-methods-tutorial</name>
    <description>Learn SSO vs OAuth vs OIDC vs SAML through tests, with Hexagonal Architecture and DDD</description>

    <properties>
        <!-- 使用 JDK 23 -->
        <java.version>23</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <archunit.version>1.3.0</archunit.version>
        <testcontainers-keycloak.version>3.4.0</testcontainers-keycloak.version>
        <!--
          固定 docker-java 對 Docker daemon 協商的 API 版本。
          新版 Docker Engine（25.0+ / API 1.44+）已不再支援 docker-java 預設協商的
          1.32，否則整合測試會出現「client version 1.32 is too old」。
          如需相容更舊的 daemon，可用 -Ddocker.api.version=<版本> 覆寫。
        -->
        <docker.api.version>1.44</docker.api.version>
    </properties>

    <dependencies>
        <!-- Web：提供 REST 「驅動轉接器 (driving adapter)」入口 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Validation：在轉接器層做輸入驗證 (Bean Validation) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- JSON：REST 回應與 SAML demo codec 使用 Jackson -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-json</artifactId>
        </dependency>

        <!-- Actuator：提供 /actuator/health 供容器/ K8s 健康檢查 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!--
          OAuth2 Resource Server：帶入 Nimbus JOSE + JWT 解析能力，
          供 OIDC 的「驅動端轉接器」驗證 ID Token (JWT) 簽章用。
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>

        <!-- ============================ 測試 ============================ -->

        <!-- Spring Boot 測試（JUnit 5 + Mockito + AssertJ） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!--
          Spring Boot 4 將 MockMvc / @WebMvcTest 的測試切片支援獨立成此模組
          （提供 @AutoConfigureMockMvc）。
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- ArchUnit：用測試「強制」六角形架構的相依規則 -->
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <version>${archunit.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Testcontainers：以真實 Keycloak 容器做 OIDC 整合測試 -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.github.dasniko</groupId>
            <artifactId>testcontainers-keycloak</artifactId>
            <version>${testcontainers-keycloak.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>1.20.4</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

            <!--
              Surefire：預設只跑「不需要 Docker」的單元 / 切片 / 架構測試，
              讓 `mvn test` 在任何環境都能綠燈通過。
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <excludedGroups>integration</excludedGroups>
                </configuration>
            </plugin>

            <!--
              Failsafe：需要 Docker 的整合測試（@Tag("integration")）放在
              `mvn verify -Pintegration` 才執行。
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- 啟用此 profile 後，整合測試 (*IT) 會在 `mvn verify -Pintegration` 執行 -->
        <profile>
            <id>integration</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-failsafe-plugin</artifactId>
                        <configuration>
                            <groups>integration</groups>
                            <!-- 讓 Testcontainers/docker-java 對新版 Docker daemon 用相容的 API 版本 -->
                            <systemPropertyVariables>
                                <api.version>${docker.api.version}</api.version>
                            </systemPropertyVariables>
                        </configuration>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>integration-test</goal>
                                    <goal>verify</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
